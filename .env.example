# =============================================================
# Restaurant Reservation System — Variables de entorno
# =============================================================
# Copia este archivo como .env y completa tus valores reales.
# ¡NUNCA subas .env al repositorio!

# PostgreSQL
DB_URL=jdbc:postgresql://localhost:5432/restaurante
DB_USERNAME=postgres
DB_PASSWORD=tu_contraseña_aqui

# JPA (opcional: "update" | "validate" | "none")
JPA_DDL_AUTO=update
